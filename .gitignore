/.phpunit.cache
/bootstrap/ssr
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder
/vendor
.DS_Store
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
/backups
/.mcp.json

# Environment files — every variant. The two committed templates are the
# only exceptions; a real .env must never be committed or pushed.
.env
.env.*
!.env.example
!.env.docker.example

# Database dumps and restores (see db-backup.sh; /backups is ignored above)
*.sql
*.sql.gz
*.dump
*.sqlite
*.sqlite-journal

# Credentials and keys
*.pem
*.p12
*.pfx
/storage/oauth-*.key

# Per-machine agent/tool state
/.superpowers
.claude/settings.local.json

# Host-local Caddy site blocks (other apps on the same box)
/docker/caddy/conf.d/*
!/docker/caddy/conf.d/README.md
